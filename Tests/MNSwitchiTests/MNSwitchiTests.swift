import XCTest
@testable import MNSwitchi

final class MNSwitchiTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MNSwitchi().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
